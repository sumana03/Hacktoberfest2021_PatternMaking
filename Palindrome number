# A program in Python to check whether a number is Palindrome number or not.

num=int(input("Enter a number : "))
temp=num
rev=0
while(num>0):
  dig=num%10
  rev=rev*10+dig
  num=num//10
if(temp==rev):
  print("The number is Palindrome.")
else:
  print("The number is not Palindrome.")
 
